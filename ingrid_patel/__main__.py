# ingrid_patel/__main__.py 

from ingrid_patel.app import run # Import the run function from the app module

if __name__ == "__main__": # Only run() if this script is executed directly
    run() # Call the run function to start the application
